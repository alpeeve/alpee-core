# TODO
- [ ] **Управление процессами модулей (`POST /api/modules/:name/start`, `POST /api/modules/:name/stop` и `GET /api/modules/:name/status`):**
  - Позволяет фронтенду вручную запускать или перезапускать упавшие модули (через вызов методов `ModuleManager`).
- [ ] **API для логов**
  - позволяет Frontend просматривать  логи ядра и модулей 
- [ ] **API для декларативных элементов UI**
  - позволяет передавать Frontend поддерживаемые элементы UI 
- [ ] **Интеграция с Lua/python** 
  - позволит создавать простые  модули на Lua/python 
- [ ] **Rust SDK для модулей**
  - упростит написание модулей